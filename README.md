pip install torch torchvision scikit-learn "numpy>=1.17.3,<1.24" ultralytics firebase_admin</br>
cd Go_bot/src/ && git clone https://github.com/ros-drivers/usb_cam.git</br>
sudo apt-get install v4l-utils<br>